CREATE TABLE cliente
(
    cod_cli       NUMERIC(5,0)      CONSTRAINT cp_cli PRIMAR KEY,
    nombre        VARCHAR(100),
    direccion     VARCHAR(100),
    cp            NUMERIC(5,0),
    cod_pueblo    NUMERIC(5,0)      CONSTRAINT ce_cli_pueblo REFERENCES pueblo
 );
 
 
